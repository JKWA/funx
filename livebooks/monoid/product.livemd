# Funx.Monoid.Product

```elixir
Mix.install([
  {:funx, "0.8.2"}
])
```

A Monoid implementation for products.

This monoid uses multiplication as its associative operation
and `1` as the identity element.

## Function Examples

```elixir
import Funx.Monoid.Product
alias Funx.Monoid.Product
```