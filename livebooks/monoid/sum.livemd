# Funx.Monoid.Sum

```elixir
Mix.install([
  {:funx, "0.7.0"}
])
```

A Monoid implementation for sums.

This monoid uses addition as its associative operation
and `0` as the identity element.

## Function Examples

```elixir
import Funx.Monoid.Sum
alias Funx.Monoid.Sum
```