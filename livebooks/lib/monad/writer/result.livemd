# Funx.Monad.Writer.Result

```elixir
Mix.install([
  {:funx, "~> 0.1.5"}
])
```

## Overview

Represents the result of running a Writer computation:
the final value and the accumulated monoid.

## Function Examples

```elixir
import Funx.Monad.Writer.Result
alias Funx.Monad.Writer.Result
```